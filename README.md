# Camera Letter Recognition
Client-side inference with TensorFlow.js and camera input. Model files (model.json and .bin) must live in `public/`.

How to run locally:
1. npm install
2. npm run dev

Deploy:
- Connect this repo to Vercel for automatic deploys.
